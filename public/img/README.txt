avatar-hex.png — DONE. Extracted from the "Yash Awachar" <img alt> in
the original portfolio-1.html (it was embedded as a base64 WebP
data-URI, converted to PNG here since the code references .png).

og-image.jpg — STILL MISSING. Unlike the avatar, this one was never
embedded as a data-URI in portfolio-1.html — the <meta property="og:image">
tag points at https://yash1318.netlify.app/img/og-image.jpg, i.e. it
already lives as a real file on the current Netlify deployment. Pull it
from there (or wherever the original image source lives) and drop it
here before the next deploy, or social share previews will 404.
